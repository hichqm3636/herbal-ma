{
  "name": "herbal-ma",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^22.19.0"
  },
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "build": "astro build",
    "preview": "astro preview",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "validate:content": "astro sync",
    "validate": "npm run format:check && npm run validate:content && npm run check && npm run build"
  },
  "dependencies": {
    "astro": "^7.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "prettier": "^3.9.8",
    "prettier-plugin-astro": "^1.0.1",
    "typescript": "~6.0.3",
    "yaml": "^2.9.1"
  }
}
